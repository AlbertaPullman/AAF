node_modules/
dist/
build/
.env
.env.local
*.log
data/sqlite/*.db
data/sqlite/*.sqlite
data/backups/*
!data/backups/.gitkeep
data/uploads/*
!data/uploads/.gitkeep
!data/uploads/avatars/.gitkeep
!data/uploads/maps/.gitkeep
!data/uploads/audio/.gitkeep
OLD AAF/

# ── temp / scratch ──
tmp/
*.tsbuildinfo

# ── sensitive design source (keep local only) ──
规则书.txt
规则书.docx

# ── Claude Code local state (worktree refs and per-machine settings) ──
.claude/settings.local.json
.claude/worktrees/
